import { gh, isExecute, vueDefineComponent } from '@syncfusion/ej2-vue-base';
import { isNullOrUndefined } from '@syncfusion/ej2-base';

export let {{arrayDirectiveClass}}Directive =  vueDefineComponent({
    inject: { custom: { default: null } },
    render(createElement: any): void {
        if (!isExecute) {
            let h: any = !isExecute ? gh : createElement;
            let slots: any = null;
            if(!isNullOrUndefined((this as any).$slots.default)) {
                slots = !isExecute ? (this as any).$slots.default() : (this as any).$slots.default;
            }
            return h('div', { class: 'e-directive' }, slots);
        }
        return;
    },
    updated(): void {
        if (!isExecute && this.custom) { this.custom() }
    },
    methods: {
        getTag(): string {
            return 'e-{{arrayDirective}}';
        }
    }
});
export const {{arrayDirectiveClass}}Plugin = {
    name: 'e-{{arrayDirective}}',
    install(Vue: any) {
        Vue.component({{arrayDirectiveClass}}Plugin.name, {{arrayDirectiveClass}}Directive);
    }
}

{{tagComment}}
export let {{DirectiveClass}}Directive =  vueDefineComponent({
    render(): void {
        return;
    },
    methods: {
        getTag(): string {
            return 'e-{{Directive}}';
        }
    }
});
export const {{DirectiveClass}}Plugin = {
    name: 'e-{{Directive}}',
    install(Vue: any) {
        Vue.component({{DirectiveClass}}Plugin.name, {{DirectiveClass}}Directive);
    }
}